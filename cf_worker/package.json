{
  "name": "pqready-mcp-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies pqready MCP traffic to a backend running the fastmcp server.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
